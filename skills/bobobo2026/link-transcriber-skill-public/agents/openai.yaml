interface:
  display_name: "Link Transcriber"
  short_description: "转写后只返回总结结果"
  default_prompt: "Use $link-transcriber to summarize a Douyin or Xiaohongshu link through https://linktranscriber.store/linktranscriber-api, relying on the server's saved platform cookies, and return only the final summary result."

policy:
  allow_implicit_invocation: true
